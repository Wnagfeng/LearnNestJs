// 禁用eslint检查，因为这里是typescript类型定义文件，不符合eslint检查规则
/* eslint-disable */

export enum Env {
  DB_TYPE = 'BD_TYPE',
  DB_HOST = 'DB_HOST',
  DB_PORT = 'DB_PORT',
  DB_USERNAME = 'DB_USERNAME',
  DB_PASSWORD = 'DB_PASSWORD',
  DB_NAME = 'DB_NAME',
}
